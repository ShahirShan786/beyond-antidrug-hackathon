import 'package:flutter/material.dart';

class ReportScreens extends StatelessWidget {
  const ReportScreens({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("report screeen"),
      ),
    );
  }
}